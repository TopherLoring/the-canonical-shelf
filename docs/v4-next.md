# Immediate v4 integration queue

1. Correct `v4-course-map.js` so the placement table contains the exact 69 legacy IDs once each.
2. Update `v4-loader.js` to load all eight mastery content files plus progress/topic bridge assets before the app shell.
3. Update v4 architecture tests to execute all content files.
4. Wire v4 progress into the preview shell instead of its temporary in-memory sets.
5. Add the v4 loader to the branch-only existing expansion loader.
6. Expand Topics coverage and glossary.
7. Migrate/reorganize the 70 guided lessons into the 23-unit sequence.
8. Replace the old guided game renderer with v4 boards.
