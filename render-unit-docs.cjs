// Generate readable unit documents from the same baseline + expansion data used by production.
const fs=require('node:fs'),vm=require('node:vm'),path=require('node:path');
const root=path.resolve(__dirname,'../public'),context={window:{},console};vm.createContext(context);
for(const file of ['foundations-data.js','foundations-expansion-core.js','foundations-units-02-08.js','foundations-units-09-16.js','foundations-expansion-meta.js'])vm.runInContext(fs.readFileSync(path.join(root,file),'utf8'),context,{filename:file});
const data=context.window.FOUNDATIONS_DATA,corpus=fs.readFileSync(path.join(root,'corpus.txt'),'utf8').trim().split(/\r?\n/).map(line=>line.split('\t'));
const passage=ref=>{const[b,c,s,e]=ref;return corpus.filter(r=>+r[0]===b&&+r[1]===c&&+r[2]>=s&&+r[2]<=e).map(r=>`**${r[2]}** ${r.slice(3).join('\t')}`).join('\n\n');};
function challenges(items){return items.map(c=>{let text=`### ${c.title}\n\n**Format:** ${c.kind}\n\n${c.prompt}\n\n`;if(c.kind==='scenario')for(const stage of c.stages)text+=`**${stage.prompt}**\n\n`+stage.choices.map((x,i)=>`- ${x}${i===stage.correct?' — intended response':''}`).join('\n')+`\n\n${stage.feedback[stage.correct]}\n\n`;else text+=c.items.map((x,i)=>`- ${x}${c.kind==='match'?' → '+c.options[c.answer[i]]:''}`).join('\n')+'\n\n';if(c.kind==='argument')text+='**Supported connections:**\n\n'+c.answer.map(([a,b])=>'- '+c.items[a]+' → '+c.items[b]).join('\n')+'\n\n';if(c.kind==='sequence')text+='**Sequence:** '+c.answer.map(i=>c.items[i]).join(' → ')+'\n\n';if(c.kind==='evidence')text+='**Supported evidence:** '+c.answer.map(i=>c.items[i]).join('; ')+'\n\n';return text+`**Hint:** ${c.hint}\n\n**Explanation:** ${c.why}\n`;}).join('\n');}
for(const unit of data.units){const lessons=data.lessons.filter(l=>l.unit===unit.id);let text=`# Unit ${unit.id}: ${unit.title}\n\n${lessons.length} complete lessons with full Berean Standard Bible readings and interactive understanding missions.\n`;for(const l of lessons){text+=`\n## ${l.title}\n\n**Objective:** ${l.objective}\n\n### ${l.reading} · Berean Standard Bible\n\n${passage(l.ref)}\n\n${l.body.join('\n\n')}\n\n**Simply:** ${l.simple}\n\n### Vocabulary\n\n`+Object.entries(l.vocab).map(([k,v])=>`- **${k}:** ${v}`).join('\n')+`\n\n**Deeper:** ${l.deeper}\n\n**Optional reflection:** ${l.reflect}\n\n**Model reflection:** ${l.model}\n\n`+challenges(l.challenges);for(const extra of l.extraReadings||[])text+=`\n### Additional reading: ${extra.reading} · Berean Standard Bible\n\n${passage(extra.ref)}\n`;if(l.comparisons)text+='\n### Theme investigations\n\n'+l.comparisons.map(c=>`**${c.title} — ${c.reading}**\n\n${c.prompt}`).join('\n\n')+'\n';if(l.reviewChallenges)text+='\n### Return mission\n\n'+challenges(l.reviewChallenges);if(l.sources)text+='\n### Sources\n\n'+l.sources.map(url=>`- [${data.sources.find(s=>s.url===url)?.title||url}](${url})`).join('\n')+'\n';}fs.writeFileSync(path.join(root,`docs/unit-${String(unit.id).padStart(2,'0')}.md`),text);}
