const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),assert=require('node:assert/strict');const pub=path.resolve(__dirname,'../public'),ctx={window:{},console};vm.createContext(ctx);for(const f of ['v4-course-map.js','v4-course-map-fixed.js'])vm.runInContext(fs.readFileSync(path.join(pub,f),'utf8'),ctx);const p=Object.values(ctx.window.CANON_V4_COURSE.masteryPlacement).flat();assert.equal(p.length,69);assert.equal(new Set(p).size,69);for(let i=1;i<=23;i++)assert.equal(ctx.window.CANON_V4_COURSE.masteryPlacement[i].length,3);console.log('PASS exact 69 mastery placements across 23 units');
