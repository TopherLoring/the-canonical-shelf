const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),assert=require('node:assert/strict');const ctx={window:{}};vm.createContext(ctx);vm.runInContext(fs.readFileSync(path.resolve(__dirname,'../public/v4-guided-game-adapter.js'),'utf8'),ctx);const A=ctx.window.CanonV4GuidedAdapter;assert.equal(A.adapt({kind:'sequence',items:[],answer:[]}).kind,'sequence-path');assert.equal(A.adapt({kind:'match',items:[],answer:[]}).kind,'match-board');assert.equal(A.adapt({kind:'argument',items:[],answer:[]}).kind,'argument-map');const e=A.adapt({kind:'evidence',items:['a','b','c'],answer:[0,2]});assert.equal(e.kind,'evidence-lab');assert.deepEqual(Array.from(e.answer,x=>Array.from(x)),[[0,0],[1,2],[2,0]]);console.log('PASS guided challenges adapt to v4 boards');
