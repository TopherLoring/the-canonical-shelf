name: application verification
on:
  push:
    branches: [curriculum-v4-redesign, v5-application-experience]
  pull_request:
    branches: [main]
    paths:
      - 'public/index.html'
      - 'public/foundations*.js'
      - 'public/v4-*'
      - 'public/v5-*'
      - 'public/topics*'
      - 'public/sw.js'
      - 'tests/**'
      - 'scripts/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/v4-curriculum.yml'
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install test dependencies
        run: npm ci
      - name: Verify full application and curriculum
        run: npm test
